import java.util.*;

public class LocalSearch
{
	int edges, vertices;
	Set <String> setEdges;
	Set <String> setVertices;
	
	public LocalSearch (int edg int vert, Set setE, Set setV)  //constructor
	{ 
		edges = edg;
		vertices = vert;
		setEdges = setE;
		setVertices = setV;
	}
	
	 /*Local Search Algorithm
     * Input: 
     * 		- arrVert: the set of variables  
     * 		- setVertices: the domain for each variable
     * 		- C: set of constraints
     * Output: 
     * 		A complete assignment
     * Extra:
     * 		- arrLoc[V] will be a local array of values indexed by V
     */
	
	
}